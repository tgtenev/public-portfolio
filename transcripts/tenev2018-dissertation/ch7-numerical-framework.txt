Chapter Seven. Numerical Framework for Weak Gravity Dynamics.

Chapter Seven of the dissertation An Elastic Constitutive Model of Spacetime and Its Applications, by Tichomir G. Tenev, Mississippi State University, 2018, held in the public portfolio store as the document of record. The abstract, errata, and table of contents are in the dissertation's landing page, which carries its own audio transcript.

Note for this audio edition. This is a listening adaptation of the written document. Mathematical expressions are read out in words, acronyms are expanded, figures and tables are described verbally, and links and references are named descriptively rather than read. The written original is the document of record.

Earlier chapters detailed the theoretical basis that enables one to use the numerical tools of solid mechanics for solving problems within General Relativity and cosmology. Herein, we apply these ideas as we contribute with a numerical framework that is suited for a category of problems in cosmology, namely those concerning weak gravitational dynamics at galactic length scales.

The Computational Methods section of Chapter One discussed a number of mainstream computational techniques, such as the Finite Element Method, the Finite Difference Method, and the N-Body Method, which find a variety of applications in Solid Mechanics, General Relativity, and Cosmology. To this list, we add the Particle Mesh Method, which is a combination of the Finite Difference Method and the N-Body Method, where the bodies do not interact with each other directly but through the gravitational potential. In turn, the latter is computed using the Finite Difference Method to solve a boundary value problem based on the Poisson equation of gravity.

Nearly all numerical methods consist of the following stages: preprocessing, solving, and post-processing. Also, most computational methods include a discretization strategy for reducing the solution domain from an infinite to a finite number of parameters. The preprocessing stage maps the physical problem onto a mathematical model, which usually involves configuring the geometry of the simulated bodies and their initial and boundary conditions. Typically, such a mathematical model is an idealization of the original problem. For example, when modeling the deformation of a body, not all surface details of the body are represented in the model, but only what is considered relevant. Likewise, when modeling the large-scale structure formation of the cosmos, stars and even galaxies may be treated as point particles. A divide-and-conquer strategy is often applied during the preprocessing stage, such as defining an element mesh in the case of the Finite Element Method and the Finite Difference Method, or using a nested set of cells for the N-Body Method. The solving stage computes the result of the numerical simulation. Iterative solvers, such as those used in the Finite Difference Method and the N-Body Method, compute the result incrementally, so that a growing portion of the result becomes available before the computation terminates. By contrast, direct solvers, such as those used with the Finite Element Method, compute the result in one big step by, for example, solving a system of linear equations, so that any part of the result only becomes available upon the completion of the computation. Finally, the post-processing stage is when the computation result is analyzed and visualized.

This chapter introduces a MATLAB-based framework that we have developed for performing numerical experiments in the category of weak gravity dynamics, under the conditions of non-flat inherent geometry of space. The chapter also includes several such experiments that illustrate the use of the framework and whose results support the Inherent Structure Hypothesis introduced in Chapter Six. Our framework is based on the Particle Mesh method, modified to account for the presence of inherent intrinsic curvature of space. The numerical method described here assumes weak gravity, that is, small strain, and nearly static gravitational fields, that is, low strain rates. As stated in Chapter One, these assumptions are valid for our common experience of gravity. They are also consistent with the conditions under which the Dark Matter effect, see Chapter Six, has been observed, such as the conditions in the peripheries of galaxies and in galactic clusters.

The weak gravity, small strain, assumption allows us to reduce what is generally a four-dimensional problem to a three-dimensional one, and solve it with conventional three-dimensional numerical tools. That is because, under the small strain assumption, the displacement of grid nodes is not a significant factor in the calculations and can be ignored. Geometric quantities, such as the hyperspatial displacement, w, the undeformed metric, g-bar-sub-i-j, and the Christoffel symbols, Gamma-bar-superscript-k-sub-i-j, are treated as generic field quantities whose discrete values have been assigned to the nodes of a fixed grid. The metric and Christoffel symbols pertaining to spacetime and to deformed space are also treated in the same way. Although grid nodes are not displaced during deformation, by accounting for these geometric quantities at each node, we are still taking into consideration the undeformed curvature.

The terms mesh and grid are used interchangeably in this chapter. Typically the former refers to any general subdivision of a body into smaller elements whose vertexes are the nodes of the mesh, while the latter implies a rectangular organization of the nodes. The framework described here is based on a rectangular grid of nodes, in which context both terms have essentially the same meaning.

The broad contributions of the work presented in this chapter include, first, the modifications to the Particle Mesh method that generalize it for the case of non-flat space, and second, the specific numerical experiments that showcase the framework and demonstrate that the Inherent Structure Hypothesis is a plausible explanation for the Dark Matter effect.

In the following sections, we describe the mathematical underpinnings of the numerical framework, in the Mathematical Model and Discretization section, and what it does, in the Numerical Framework section, and in the Numerical Experiments section we apply it to specific numerical experiments. In the Discussion section we comment on the important factors for the design of the framework and the experiments that are based on it. Finally, we summarize and conclude in the Summary and Conclusion section.

Mathematical Model and Discretization.

Our setup consists of a three-dimensional rectangular grid of nodes and a set of particles within the grid, see Figure 7.1. Various field quantities, such as density and curvature, can be assigned to each node of the grid and subsequently deduced for an arbitrary point in space by spline interpolation from the nearby node quantities. The inherent intrinsic curvature of space is specified via a displacement function, w-bar equals w-bar of x-superscript-i, like the one discussed in Chapter Six and visualized in Figure 6.1, except that here the displacement function is not necessarily spherically symmetric, but can vary independently in each coordinate direction.

Figure 7.1, described in words. The figure shows a three-dimensional rectangular grid of nodes, drawn as round dots, together with a scattering of particles among them, drawn as larger hollow disks. The caption reads: the grid nodes, indexed as a, b, c, are represented with round dots, while particles, indexed with subscript p, are represented as larger hollow disks. The indexes i, j, k, running from one to three, run over the three spatial dimensions. Some of the quantities associated with the grid nodes and particles are also represented, such as: x-superscript-k-sub-a-b-c, the node position; g-sub-i-j evaluated at x-superscript-k-sub-a-b-c, the metric tensor at that location; x-superscript-i-sub-p, the position of the p-th particle; and m-sub-p, the mass of the p-th particle. The spacing h-superscript-one-sub-a between the grid nodes in dimension one varies along that dimension, but remains constant along the remaining dimensions, and likewise for the spacings h-superscript-two-sub-b and h-superscript-three-sub-c.

We will use the following notation. Latin subscripts in the alphabet range of a, b, c are used to denote the grid indexes of the node to which the subscripted quantity applies, where a runs from one to a-max, b runs from one to b-max, and c runs from one to c-max, on a grid of size a-max by b-max by c-max, see Figure 7.1. For example, rho-sub-a-b-c means the mass density at grid node a, b, c. The Latin subscript p specifies the particle index. For example, m-sub-p is the mass of the p-th particle, where p runs from one to p-tot, and p-tot is the total number of particles. Latin indexes in the alphabet range of i, j, k, l, running from one to three, correspond to the spatial components of vectors and tensors. For example, x-superscript-i-sub-a-b-c refers to the three spatial coordinates for the grid node a, b, c. Similarly, x-superscript-i-sub-p are the three spatial coordinates of the p-th particle. Note that rho-sub-a-b-c is equivalent to rho evaluated at x-superscript-i, because, being a field quantity, rho is a function of the position x-superscript-i. This alternative way of denoting a field quantity helps with tensor notation, where writing the node indexes as subscripts to the tensor quantity could be awkward. For example, g-sub-i-j evaluated at x-superscript-k-sub-a-b-c denotes the value of the metric tensor g-sub-i-j at the node a, b, c. As usual, Greek indexes myu, nyu, alpha, beta, running from zero to three, run over the four spacetime dimensions, with the zeroth dimension corresponding to time.

We consider a grid of dimensions a-max by b-max by c-max, for which the spacing between the grid nodes is not necessarily uniform, but is specified by the differences h-superscript-one-sub-a, h-superscript-two-sub-b, and h-superscript-three-sub-c, where a runs from one to a-max minus one, and likewise for b and c. In other words, x-superscript-one-sub-a-b-c equals x-superscript-one-sub-a-minus-one-b-c, plus h-superscript-one-sub-a-minus-one, and likewise for x-superscript-two-sub-a-b-c and x-superscript-three-sub-a-b-c, see Figure 7.1. The non-uniform spacing of the grid is useful when higher fidelity is needed at some locations but not others, such as the center of a gravitational system compared to its outskirts.

The following are the two governing differential equations, which are based on the Cosmic Fabric analogy of General Relativity, under the conditions of weak gravity, small strain, and slowly changing fields.

x-double-dot-superscript-alpha, plus Gamma-superscript-alpha-sub-myu-nyu, times x-dot-superscript-myu, times x-dot-superscript-nyu, equals zero. This is Equation 7.1.

The covariant Laplacian of epsilon equals minus one-half times c squared times kappa times rho. This is Equation 7.2.

where rho, epsilon, kappa, and c are, respectively, the matter density, the cosmic fabric volumetric strain, the Einstein constant, and the speed of light. Also, nabla-sub-i is the covariant gradient operator, so that nabla-superscript-i nabla-sub-i represents the covariant Laplacian, and Equation 7.2 is the covariant Poisson equation. The dot notation stands for differentiation with respect to proper time. Equation 7.1 is that of a geodesic in spacetime, which is the generalization of a straight line to curved space. This equation states mathematically the generalization of Newton's First Law of motion, namely, that a particle free of any forces moves along a geodesic in spacetime. Equation 7.2, which is the same as Equation 3.2, copied here for convenience, is the Inclusion Postulate of the Cosmic Fabric model. Equations 7.1 and 7.2 reflect General Relativity's two-part view of gravity, see the General Relativity section of Chapter Two, namely that, first, bent space causes matter to accelerate, and second, matter bends space.

Equations 7.1 and 7.2 relate to each other via the metric tensor, through the following geometric identity.

g-sub-alpha-beta times Gamma-superscript-alpha-sub-myu-nyu equals one-half times the quantity partial-sub-nyu g-sub-myu-beta, plus partial-sub-myu g-sub-beta-nyu, minus partial-sub-beta g-sub-myu-nyu. This is Equation 7.3.

and through the Time Lapse postulate of the Cosmic Fabric, which for weak gravity can be stated as follows, see Equation 3.7.

g-sub-zero-zero is approximately equal to minus one plus two epsilon. This is Equation 7.4.

where epsilon is the volumetric strain of the fabric.

In the following subsections, we convert the above equations to a discretized form suitable for use in a numerical simulation. The goal is to be able to compute particle accelerations, so that we can evolve the system starting from a set of initial particle positions and velocities. We begin by developing the technique for discretizing first and second derivatives. Next we use this technique to discretize the metric and Christoffel symbols, the geodesic equation, Equation 7.1, and the covariant Poisson equation, Equation 7.2. Finally, we discuss how the density field quantity rho is computed for each grid node from the particle masses.

Discretization of First and Second Derivatives.

We will use a three-point stencil along each dimension to discretize the first and second partial derivatives. Following the approach outlined by Sundqvist and Veronis, let f of x-superscript-i be a field quantity, and let f-sub-a-b-c equal f evaluated at x-superscript-i-sub-a-b-c, be the specific values of this quantity assigned to each grid node. We seek to compute the values partial-sub-i f-sub-a-b-c, and partial-sub-i-j f-sub-a-b-c. Below, we do so using concrete values for i and j, from which the generalization to any values of i and j, running from one to three, should be clear.

First, we compute the single and double derivatives, partial-sub-one and partial-sub-one-one, respectively, for i equal to one. Using the Taylor series expansions for f-sub-a-minus-one-b-c and f-sub-a-plus-one-b-c around the point x-sub-a-b-c, we obtain the following two equations.

f-sub-a-minus-one-b-c equals f-sub-a-b-c, minus h-superscript-one-sub-a-minus-one times partial-sub-one f-sub-a-b-c, plus the quantity h-superscript-one-sub-a-minus-one, squared, over two, times partial-sub-one-one f-sub-a-b-c, minus the quantity h-superscript-one-sub-a-minus-one, cubed, over six, times partial-sub-one-one-one f-sub-a-b-c, plus terms of order h to the fourth. This is Equation 7.5.

f-sub-a-plus-one-b-c equals f-sub-a-b-c, plus h-superscript-one-sub-a times partial-sub-one f-sub-a-b-c, plus the quantity h-superscript-one-sub-a, squared, over two, times partial-sub-one-one f-sub-a-b-c, plus the quantity h-superscript-one-sub-a, cubed, over six, times partial-sub-one-one-one f-sub-a-b-c, plus terms of order h to the fourth. This is Equation 7.6.

The third-power term in the expansion is included to convey the error due to the approximation. By first solving the above two equations for partial-sub-one f-sub-a-b-c, and then for partial-sub-one-one f-sub-a-b-c, we obtain the following results.

partial-sub-one f-sub-a-b-c equals d-superscript-one-sub-a times f-sub-a-plus-one-b-c, minus e-superscript-one-sub-a times f-sub-a-minus-one-b-c, plus the quantity e-superscript-one-sub-a minus d-superscript-one-sub-a, times f-sub-a-b-c, minus the quantity h-superscript-one-sub-a-minus-one times h-superscript-one-sub-a, over six, times partial-sub-one-one-one f-sub-a-b-c, plus terms of order h to the fourth. This is Equation 7.7.

partial-sub-one-one f-sub-a-b-c equals two times the quantity: d-superscript-one-sub-a, over h-superscript-one-sub-a-minus-one, times f-sub-a-plus-one-b-c, plus e-superscript-one-sub-a, over h-superscript-one-sub-a, times f-sub-a-minus-one, minus the quantity d-superscript-one-sub-a over h-superscript-one-sub-a-minus-one, plus e-superscript-one-sub-a over h-superscript-one-sub-a, times f-sub-a-b-c; minus the quantity h-superscript-one-sub-a minus h-superscript-one-sub-a-minus-one, over three, times partial-sub-one-one-one f-sub-a-b-c, plus terms of order h to the fourth. This is Equation 7.8.

where we have made the following substitutions. d-superscript-one-sub-a is defined as h-superscript-one-sub-a-minus-one, over the quantity h-superscript-one-sub-a-minus-one plus h-superscript-one-sub-a, times h-superscript-one-sub-a, for a running from two up to a-max minus one; d-superscript-one-sub-one is defined as one over h-superscript-one-sub-one; and d-superscript-one-sub-a-max is defined as zero. Likewise, e-superscript-one-sub-a is defined as h-superscript-one-sub-a, over the quantity h-superscript-one-sub-a-minus-one plus h-superscript-one-sub-a, times h-superscript-one-sub-a-minus-one, for the same range of a; e-superscript-one-sub-one is defined as zero; and e-superscript-one-sub-a-max is defined as one over h-superscript-one-sub-a-max-minus-one. This is Equation 7.9.

After dropping the higher-order terms, we can write the finite-difference approximations of the first and second derivative in terms of the operators Delta-sub-one and Delta-sub-one-one, defined as follows.

partial-sub-one f-sub-a-b-c is approximately equal to Delta-sub-one f-sub-a-b-c, which is defined as d-superscript-one-sub-a times f-sub-a-plus-one-b-c, minus e-superscript-one-sub-a times f-sub-a-minus-one-b-c, plus the quantity e-superscript-one-sub-a minus d-superscript-one-sub-a, times f-sub-a-b-c. This is Equation 7.10.

partial-sub-one-one f-sub-a-b-c is approximately equal to Delta-sub-one-one f-sub-a-b-c, which is defined as two times the quantity: d-superscript-one-sub-a over h-superscript-one-sub-a-minus-one, times f-sub-a-plus-one-b-c, plus e-superscript-one-sub-a over h-superscript-one-sub-a, times f-sub-a-minus-one, minus the quantity d-superscript-one-sub-a over h-superscript-one-sub-a-minus-one, plus e-superscript-one-sub-a over h-superscript-one-sub-a, times f-sub-a-b-c. This is Equation 7.11.

The discretization of partial-sub-i and partial-sub-i-i for i equal to two and three can be computed analogously. The approximation of the second derivative in Equation 7.11 is known as the central-differencing scheme.

Equations 7.10 and 7.11 show that the discretized derivative for a value at a node is some linear combination of the values at adjacent nodes. Based on this observation, we can replace the finite-difference operators Delta-sub-i and Delta-sub-i-i with linear ones. Let us define the following. Here, n and m index a single flattened list of grid nodes, so any quantity written with two such subscripts is a matrix entry: a row for n and a column for m.

n-sub-a-b-c is defined as one, plus a-max times b-max times the quantity c minus one, plus a-max times the quantity b minus one, plus the quantity a minus one; that is, a single running index that enumerates all the grid nodes in sequence. N-max is defined as a-max times b-max times c-max, the total number of nodes. F-sub-n-a-b-c is defined as f-sub-a-b-c, the field values reindexed by the single running index n instead of by the three grid indexes a, b, and c. Using this indexing, the first-derivative operator D-superscript-one, as a matrix indexed by n and m, is defined entry by entry: the entry at row n-sub-a-b-c and column n-sub-a-plus-one-b-c equals d-superscript-one-sub-a; the entry at row n-sub-a-b-c and column n-sub-a-minus-one-b-c equals minus e-superscript-one-sub-a; and the diagonal entry at row and column n-sub-a-b-c equals e-superscript-one-sub-a minus d-superscript-one-sub-a. Likewise, the second-derivative operator D-superscript-one-one is defined entry by entry: the entry at row n-sub-a-b-c and column n-sub-a-plus-one-b-c equals two d-superscript-one-sub-a over h-superscript-one-sub-a-minus-one; the entry at row n-sub-a-b-c and column n-sub-a-minus-one-b-c equals two e-superscript-one-sub-a over h-superscript-one-sub-a; and the diagonal entry equals minus two d-superscript-one-sub-a over h-superscript-one-sub-a-minus-one, minus two e-superscript-one-sub-a over h-superscript-one-sub-a. This is Equation 7.12.

and likewise for D-superscript-i, row n column m, and D-superscript-i-i, row n column m, where i equals two or three, and n and m run from one to n-max. Note that the above definitions only apply when the indexes a, b, c are within their respective bounds. So, for example, the entry of D-superscript-one connecting row n-sub-a-b-c to column n-sub-a-minus-one-b-c is only defined for a running from two to a-max. The discretization of the mixed derivative partial-sub-i-j, for i not equal to j, is simply the combination of the respective first derivatives.

For mixed derivatives with i not equal to j, the combined operator D-superscript-i-j, as a matrix indexed by n and m, equals the matrix product of D-superscript-i and D-superscript-j; that is, D-superscript-i, row n column t, times D-superscript-j, row t column m, summed over the repeated index t. This is Equation 7.13.

where summation of repeated indexes is implied.

Given the above definitions, Equations 7.10 and 7.11 can be formulated as follows. The discretized first derivative is F-superscript-i-sub-n equals D-superscript-i, row n column m, times F-sub-m, and the discretized second or mixed derivative is F-superscript-i-j-sub-n equals D-superscript-i-j, row n column m, times F-sub-m, in both cases with summation implied over the repeated index m. This is Equation 7.14.

where we have let F-superscript-i-sub-n-sub-a-b-c equal Delta-sub-i f-sub-a-b-c, and F-superscript-i-j-sub-n-sub-a-b-c equal Delta-sub-i-j f-sub-a-b-c. Notice that the D matrices only depend on the grid spacing and can therefore be precomputed during the preprocessing stage.

The method described above applies to computing field derivatives at the location of each grid node. In order to compute a field derivative of some field quantity at an arbitrary location, we would first compute the field derivative at the grid nodes, and use spline interpolation to determine the value of the derivative at the given location.

Discretization of the Undeformed Metric and Christoffel Symbols.

The undeformed metric and Christoffel symbols can be computed during the preprocessing stage from the following analytical formula.

g-bar-sub-i-j equals delta-sub-i-j, plus partial-sub-i w-bar times partial-sub-j w-bar. Gamma-bar-superscript-k-sub-i-j equals one-half times g-bar-superscript-k-l, times the quantity partial-sub-j g-bar-sub-i-l, plus partial-sub-i g-bar-sub-l-j, minus partial-sub-l g-bar-sub-i-j. This is Equation 7.15.

where w-bar equals w-bar of x-superscript-i is the displacement of the cosmic fabric into the fourth hyperspatial dimension, by which the user specifies inherent curvature, see Chapter Six. In discretized form, the above formula becomes the following.

The grid analog of the undeformed metric, G-superscript-i-j-sub-n, equals delta-sub-i-j, plus the element-wise product of two derivative results: D-superscript-i, row n column m, times W-sub-m; circle-times D-superscript-j, row n column m, times W-sub-m. The grid analog of the Christoffel symbols, J-superscript-k-i-j-sub-n, equals one-half times the matrix inverse of G-superscript-k-l-sub-n, element-wise multiplied by the quantity: D-superscript-j, row n column m, times G-superscript-i-l-sub-m; plus D-superscript-i, row n column m, times G-superscript-l-j-sub-m; minus D-superscript-l, row n column m, times G-superscript-i-j-sub-m. This is Equation 7.16.

where G-superscript-i-j-sub-n-sub-a-b-c is defined to equal g-bar-sub-i-j evaluated at the node with coordinates x-superscript-k-sub-a-b-c; W is defined to equal w-bar-sub-a-b-c; and J-superscript-k-i-j-sub-n is defined to equal Gamma-bar-superscript-k-sub-i-j evaluated at that same node. Furthermore, the operator inv applies to G's parenthesized indexes and represents a three-by-three matrix inversion; the circle operator represents element-wise multiplication with respect to the un-parenthesized index, that is, the node index; summation over repeated indexes is implied; and the D matrices are as defined above. The element-wise multiplication represented by the circle operator can be substituted with regular matrix multiplication, provided the vectors are treated as diagonal matrices whose diagonal elements are the vector's own elements.

Discretization of the Geodesic Equation.

The geodesic equation is used to compute particle acceleration for a given gravitational field. Using the chain rule, we can rewrite Equation 7.1 to be in terms of coordinate time instead of proper time.

partial-sub-zero-zero x-superscript-myu equals minus Gamma-superscript-myu-sub-alpha-beta, times partial-sub-zero x-superscript-alpha, times partial-sub-zero x-superscript-beta, plus Gamma-superscript-zero-sub-alpha-beta, times partial-sub-zero x-superscript-alpha, times partial-sub-zero x-superscript-beta, times partial-sub-zero x-superscript-myu. This is Equation 7.17.

For simulation purposes, we need to use a notion of time that is common to all particles, but proper time is by definition particle-specific. This is why we work in terms of coordinate time. Thus, partial-sub-zero-zero x-superscript-myu is the myu-th component of the particle's coordinate acceleration, while partial-sub-zero x-superscript-myu is the myu-th component of the particle's coordinate velocity. Notice that for myu equal to zero, acceleration in the time direction, both sides of Equation 7.17 vanish identically, since partial-sub-zero x-superscript-zero equals one, because the speed of coordinate time is always one second per second. In fact, we are only interested in the spatial components of the particle acceleration, partial-sub-zero-zero x-superscript-k.

In the case of slowly changing fields, the time derivatives, partial-sub-zero, of field quantities, and the time-space components of the metric, g-sub-i-zero and g-sub-zero-i, are all negligible. Consequently, some of the Christoffel symbols vanish, and the remaining ones are as follows.

Gamma-superscript-zero-sub-i-zero equals Gamma-superscript-zero-sub-zero-i, equals partial-sub-i g-sub-zero-zero, over two g-sub-zero-zero; and Gamma-superscript-zero-sub-zero-zero equals Gamma-superscript-zero-sub-i-j equals zero. Gamma-superscript-k-sub-zero-zero equals minus one-half g-superscript-k-l times partial-sub-l g-sub-zero-zero; and Gamma-superscript-k-sub-i-zero equals Gamma-superscript-k-sub-zero-i equals zero. Gamma-superscript-k-sub-i-j equals one-half g-superscript-k-l, times the quantity partial-sub-j g-sub-i-l, plus partial-sub-i g-sub-l-j, minus partial-sub-l g-sub-i-j. This is Equation 7.18.

which, when substituted into Equation 7.17 and evaluated for the spatial components of the particle acceleration, produce the following.

partial-sub-zero-zero x-superscript-k equals minus Gamma-superscript-k-sub-zero-zero, times the quantity partial-sub-zero x-superscript-zero, squared, plus two Gamma-superscript-zero-sub-zero-i, times partial-sub-zero x-superscript-k, times partial-sub-zero x-superscript-i, minus Gamma-superscript-k-sub-i-j, times partial-sub-zero x-superscript-i, times partial-sub-zero x-superscript-j; which equals one-half g-superscript-k-l times partial-sub-l g-sub-zero-zero, plus the quantity partial-sub-i g-sub-zero-zero over g-sub-zero-zero, times partial-sub-zero x-superscript-i, times partial-sub-zero x-superscript-k, minus Gamma-superscript-k-sub-i-j, times partial-sub-zero x-superscript-i, times partial-sub-zero x-superscript-j. This is Equation 7.19.

Notice that although we have assumed that the fields are slowly changing, we do not assume that particle velocities are slow, but that individual particles could achieve speeds comparable to c. Therefore, while we can approximate the time derivatives of field quantities as vanishing, we do not make the same approximation for the time derivatives of particle quantities. In a typical configuration, such as a galaxy, the total gravitational field is the combined effect of a large number of particles, billions of stars, so as long as the collection of particles as a whole moves slowly through space, then the motion of individual particles, stars, will not cause the gravitational field to change rapidly except in its immediate vicinity.

Next, we invoke the weak gravity, small strain, assumption to approximate the spatial curvature of the deformed fabric with that of the undeformed one: g-sub-i-j is approximately equal to g-bar-sub-i-j, and Gamma-superscript-k-sub-i-j is approximately equal to Gamma-bar-superscript-k-sub-i-j. Furthermore, from Equation 7.4 it follows that partial-sub-l g-sub-zero-zero is approximately equal to two partial-sub-l epsilon. Finally, we recognize that, since x-superscript-zero equals c times t, where t is coordinate time, then a-superscript-k equals c squared times partial-sub-zero-zero x-superscript-k, and v-superscript-k equals c times partial-sub-zero x-superscript-k, where a-superscript-k and v-superscript-k are, respectively, the particle's coordinate acceleration and velocity. When these substitutions are made into Equation 7.19, we arrive at the following formula for the acceleration a-superscript-k.

a-superscript-k equals c squared times g-bar-superscript-k-l, times partial-sub-l epsilon, minus the quantity two partial-sub-i epsilon, over the quantity one minus two epsilon, times v-superscript-i times v-superscript-k, minus Gamma-bar-superscript-k-sub-i-j, evaluated at x-superscript-k, times v-superscript-i times v-superscript-j. This is Equation 7.20.

Note that, in the absence of intrinsic curvature, g-bar-superscript-i-j equals delta-superscript-i-j, and for the case of slow particle velocities, v-superscript-i much less than c, the last two terms vanish. Moreover, per Equation 3.27, epsilon equals minus Phi over c squared, so Equation 7.20 becomes the classical equation for gravitational acceleration: a-superscript-k equals minus partial-superscript-k Phi of x-superscript-k. Notice also that in the case when a particle's velocity is a non-negligible fraction of the speed of light, then the second term in Equation 7.20 reduces the coordinate acceleration, which prevents the particle from achieving superluminal speed. This is why accounting for this term is important for the numerical simulation. Finally, the last term represents the influence of the inherent, undeformed, curvature on the acceleration computation for the case of fast particles.

The discretized version of the particle acceleration Equation 7.20 can now be derived, and is as follows.

The discretized particle acceleration, A-superscript-k-sub-p, equals: c squared times the matrix inverse of G-superscript-k-l-sub-n, element-wise multiplied by D-superscript-l, row n column m, times E-sub-m, then interpolated to the location of particle p; minus the quantity two times D-superscript-i, row n column m, times E-sub-m, over the quantity one minus two E-sub-n, interpolated to particle p, element-wise multiplied by V-superscript-i-sub-p and V-superscript-k-sub-p; minus J-superscript-k-i-j-sub-n, interpolated to particle p, element-wise multiplied by V-superscript-i-sub-p and V-superscript-j-sub-p. This is Equation 7.21.

where E-sub-n is an n-component vector, such that E-sub-n-sub-a-b-c equals epsilon-sub-a-b-c. Furthermore, V-superscript-k-sub-p and A-superscript-k-sub-p are, respectively, the velocity and acceleration components of the p-th particle. The notation of a quantity X with a vertical bar and subscript p means that the quantity X should be interpolated from its grid-node values for the location of the p-th particle. The division operation in the second term of the above expression should be understood as element-wise.

Discretization of the Poisson Equation.

Given the following expansion of the covariant Laplace operator in terms of the metric and Christoffel symbols:

the covariant Laplacian of epsilon equals g-bar-superscript-i-j, times the quantity partial-sub-i-j, minus Gamma-bar-superscript-k-sub-i-j times partial-sub-k, all applied to epsilon. This is Equation 7.22.

the covariant Laplacian is discretized as follows.

The discretized covariant Laplacian, L, row n column m, is defined as the matrix inverse of G-superscript-i-j-sub-n, element-wise multiplied by the quantity D-superscript-i-j, row n column m, minus J-superscript-k-i-j-sub-n, element-wise multiplied by D-superscript-k, row n column m. This is Equation 7.23.

where the matrix inversion is with respect to the upper indexes i, j.

Discretizing the Poisson Equation 7.2 requires that we take into account the following boundary conditions.

epsilon-sub-a-b-c equals epsilon-star-sub-a-b-c, for boundary nodes a, b, c. This is Equation 7.24.

where a boundary node a, b, c, as opposed to an interior node, is one for which at least one of the following is the case: a is zero or a-max, or b is zero or b-max, or c is zero or c-max. Furthermore, the strains epsilon-star-sub-a-b-c stand for user-specified boundary quantities. Consequently, we arrive at the following discretization for the Poisson equation.

Q, row n column m, times E-sub-m, equals S-sub-n. Here, the Q matrix is defined node by node: at an interior node a, b, c, the row of Q equals the corresponding row of L; at a boundary node, the diagonal entry of Q equals one. The right-hand side S is likewise defined node by node: at an interior node, S-sub-n equals minus one-half c squared kappa rho-sub-a-b-c; at a boundary node, S-sub-n equals epsilon-star-sub-a-b-c. And E-sub-m-sub-a-b-c equals epsilon-sub-a-b-c, as before. This is Equation 7.25.

Solving Equation 7.25 amounts to solving a linear system of n equations, where the matrix Q, row n column m, is sparse and banded. A banded matrix consists of a main diagonal and m pairs of secondary diagonals that are parallel to the main one, within a narrow band. An optimal solver for this type of matrix can compute the solution in a time on the order of m squared times n-max. For the case of flat geometry, m equals three, but m could be as large as thirteen for non-flat geometry. Numerical platforms, such as MATLAB, have built-in routines to perform just this type of calculation. Notice also that the matrix Q, row n column m, in Equation 7.25 can be computed during the preprocessing stage.

Density Computation.

The matter density, which figures as the source in the Poisson Equation 7.2, and in its discretized version in Equation 7.25, is computed from the following two sources: first, a predefined fixed matter density associated with each node, and second, density due to the particle distributions, which may vary from one time step to another. The former is specified directly by the user as part of setting up a numerical experiment; the computation of the latter is described below. These two are added together to produce the total density, and its value is assigned to each grid node.

Each particle contributes to the matter density value associated with the grid nodes in its immediate neighborhood. Let Omega-sub-a-b-c represent the rectangular space bounded by eight adjacent grid nodes, such that two of them are a, b, c and a-plus-one, b-plus-one, c-plus-one, and let p being a member of Omega-sub-a-b-c denote the fact that the p-th particle is located somewhere within Omega-sub-a-b-c. Define ksai-superscript-one-sub-a as x-superscript-one-sub-p minus x-superscript-one-sub-a-b-c, to denote the coordinate difference between the coordinates of the particle and the node a, b, c, and likewise for ksai-superscript-two-sub-b and ksai-superscript-three-sub-c. Therefore, the following expression, the quantity one minus ksai-superscript-one-sub-a over h-superscript-one-sub-a, times the quantity one minus ksai-superscript-two-sub-b over h-superscript-two-sub-b, times the quantity one minus ksai-superscript-three-sub-c over h-superscript-three-sub-c, represents the fraction of the particle's mass that will be ascribed to node a, b, c, while the product of ksai-superscript-one-sub-a over h-superscript-one-sub-a, ksai-superscript-two-sub-b over h-superscript-two-sub-b, and ksai-superscript-three-sub-c over h-superscript-three-sub-c, is the fraction of mass ascribed to node a-plus-one, b-plus-one, c-plus-one, and so on for the other six nodes that are the vertexes of Omega-sub-a-b-c. Note that the sum of all such fractional contributions is unity.

To compute the mass density rho-sub-a-b-c at a given node a, b, c, we will consider all of the particles' contributions from the eight adjacent spaces, Omega-sub-a-minus-one-b-minus-one-c-minus-one, Omega-sub-a-b-minus-one-c-minus-one, and so on, up to Omega-sub-a-b-c. Therefore, the following holds.

rho-sub-a-b-c equals rho-star-sub-a-b-c, plus, eight over script-V-sub-a-b-c, times the sum of terms across the eight adjacent spaces, given in braces. The first term is the sum, over particles p in Omega-sub-a-minus-one-b-minus-one-c-minus-one, of m-sub-p, times ksai-superscript-one-sub-a-minus-one over h-superscript-one-sub-a-minus-one, times ksai-superscript-two-sub-b-minus-one over h-superscript-two-sub-b-minus-one, times ksai-superscript-three-sub-c-minus-one over h-superscript-three-sub-c-minus-one. The second term is the sum, over particles p in Omega-sub-a-b-minus-one-c-minus-one, of m-sub-p, times the quantity one minus ksai-superscript-one-sub-a over h-superscript-one-sub-a, times ksai-superscript-two-sub-b-minus-one over h-superscript-two-sub-b-minus-one, times ksai-superscript-three-sub-c-minus-one over h-superscript-three-sub-c-minus-one; and so on, following the same pattern, for the remaining five of the eight adjacent spaces. The eighth and final term shown explicitly is the sum, over particles p in Omega-sub-a-b-c, of m-sub-p, times the quantity one minus ksai-superscript-one-sub-a over h-superscript-one-sub-a, times the quantity one minus ksai-superscript-two-sub-b over h-superscript-two-sub-b, times the quantity one minus ksai-superscript-three-sub-c over h-superscript-three-sub-c. Here, script-V-sub-a-b-c is defined as the square root of g-bar-sub-a-b-c, times the quantity h-superscript-one-sub-a-minus-one plus h-superscript-one-sub-a, times the quantity h-superscript-one-sub-b-minus-one plus h-superscript-one-sub-b, times the quantity h-superscript-one-sub-c-minus-one plus h-superscript-one-sub-c. This is Equation 7.26.

where script-V-sub-a-b-c represents the proper volume of the rectangular space bounded by the nodes a-minus-one, b-minus-one, c-minus-one and a-plus-one, b-plus-one, c-plus-one, that is made up of the eight smaller rectangular spaces, each of which corresponds to a summation expression in Equation 7.26. Furthermore, g-bar-sub-a-b-c is defined as the determinant of g-bar-sub-i-j evaluated at x-superscript-k-sub-a-b-c, and rho-star-sub-a-b-c is the fixed density at node a, b, c that has been pre-assigned by the user. Note that script-V-sub-a-b-c can be computed during the preprocessing stage.

We have found that the precision of the particle density calculation worsens with larger variations of the grid spacing. For such situations, we use an auxiliary grid of uniform spacing, at whose nodes we calculate the particle densities by the above scheme. Then, we interpolate the resulting values onto the nodes of the main grid. The number of nodes in this auxiliary grid can be much larger than those of the main grid, because it does not affect the solving of the Poisson equation, which is typically the most time-consuming step of the simulation.

Numerical Framework.

Using MATLAB, we implemented a numerical framework based on the mathematical model discussed in the Mathematical Model and Discretization section. Its features allow the user to define and run numerical experiments in weak gravity, under conditions of various kinds of intrinsic curvature, or none, that may involve point particles or gravitating mass densities or both. The difference between point particles and mass densities is that the former are modeled as having no spatial extent but can move, while the latter occupy finite volume but are fixed. Point particles and mass densities interact indirectly with each other through the gravitational field in whose creation they participate. The framework does not model direct interaction, such as collision or friction, between particles and densities.

There are two broad categories of numerical experiments that can be performed with the framework: first, static evaluation of gravitational fields, and second, time evolution of point particles. For experiments in the first category, the Poisson equation of gravity, Equation 7.25, is solved once, and the resulting gravitational field is analyzed. The experiments presented in the Numerical Experiments section fall into this category. For the second category of experiments, the Poisson equation is solved once for each time step. At each iteration, the volumetric strain, which is the solution of the Poisson equation, is used to compute the accelerations of the point particles and update their positions and velocities.

Input Parameters.

The following are the input parameters that specify a numerical experiment within the framework. First, the configuration of a three-dimensional rectangular mesh that has a-max by b-max by c-max nodes, and represents a volume of space that is the absolute difference between x-superscript-one-max and x-superscript-one-min, by the absolute difference between x-superscript-two-max and x-superscript-two-min, by the absolute difference between x-superscript-three-max and x-superscript-three-min, large. The nodes along each edge need not be evenly spaced, so some parts of the mesh can be finer than others. The framework allows edge nodes to be distributed linearly, quadratically, or cubically within user-specified ranges. In the latter cases, the points are denser in the middle of a range and less so near its two ends, according to a quadratic or cubic function, respectively. Second, the intrinsic curvature, defined in terms of the function w equals w of x-superscript-one, x-superscript-two, x-superscript-three, which is the displacement into the fourth, hyperspatial, dimension. Third, a set of fixed mass-density distributions, such as those representing large gravitating bodies or dust clouds. Fourth, a set of point particles specified in terms of their masses m-sub-p, where p runs from one to p-tot and p-tot is the total number of particles, and their initial positions x-superscript-i-sub-p and velocities v-superscript-i-sub-p. Fifth, boundary conditions specifying the volumetric fabric strain at the edge nodes and face nodes of the mesh. In the current implementation, the boundary values of the volumetric strain are assumed to approximately vanish, which is generally true if the mesh is made large enough that gravitating masses are near the center of the mesh.

Preprocessing Stage.

During the preprocessing stage, the framework builds the grid illustrated in Figure 7.1, and computes the derivative matrices D-superscript-i, row n column m, and D-superscript-i-j, row n column m, see the Discretization of First and Second Derivatives section, which are then used to compute the metric G-superscript-i-j-sub-n and the Christoffel symbols. Finally, the Q, row n column m, matrix is computed, see the Discretization of the Poisson Equation section, that is used in the solution of the Poisson equation. At this stage, the framework also precomputes the velocity-independent coefficients in the geodesic Equation 7.21, which is used during the solving stage to evaluate particle accelerations. Some of these preprocessing operations are computationally expensive, and being able to perform them ahead of time, rather than at each iteration during the solving stage, is important for the efficiency of the algorithm.

Solving Stage.

The solving stage begins by computing the aggregate density distribution of matter within the mesh, which consists of accounting for particle density and user-inputted fixed densities, as described in the Density Computation section. This aggregate density, from point particles and fixed density, figures in the right-hand side of the Poisson Equation 7.25. Solving Equation 7.25 is at the heart of the solving stage, and is the most computationally intensive step. MATLAB, which is the engine running our framework, uses a set of heuristics to decide the best way to solve the linear system of equations. In our case, since the Q, row m column n, matrix is banded, a banded solver is used. In the current implementation of the framework, we assume that the grid is always constructed large enough that the strain at the boundary is nearly vanishing. In other words, we assume that in Equation 7.25 the quantities epsilon-star-sub-a-b-c equal zero.

The volumetric strain field epsilon is evaluated as the solution of the Poisson equation. It is used in the computation of particle accelerations based on the geodesic Equation 7.21. What happens in the next step of the solving stage depends on the type of numerical experiment being conducted. If the goal is to study the gravitational field for a particular configuration, then the solving stage ends here. The first coefficient in Equation 7.21 describes the acceleration field for particles that are at rest, or nearly at rest, and can be used to compute, for example, galaxy rotation curves, as we have done in the numerical experiments presented in the Numerical Experiments section.

Alternatively, if the goal is to study the evolution of particles, then as its next step, the solving stage determines individual particle accelerations from Equation 7.21. For a given time step, the framework advances the particles' positions based on their velocities, and updates the particles' velocities based on the thusly computed accelerations, as the following code snippet illustrates.

The code snippet defines a function named run-step, taking the simulation object and a time step as its two arguments. Its header comment states that it runs the simulation for one time step. The function body proceeds in two stages. First, it makes three method calls on the simulation object in sequence: it computes the density, then solves the Poisson equation, then computes the particle accelerations. Second, it loops over the three spatial dimensions, and for each one updates the particle positions by adding velocity times the time step, and then updates the particle velocities by adding acceleration times the time step.

Here, obj refers to the simulation object instance, while px, pv, and pa are MATLAB's representation of the x-superscript-i-sub-p, v-superscript-i-sub-p, and a-superscript-i-sub-p vectors, corresponding, respectively, to the particles' positions, velocities, and accelerations. For example, the first component of px is represented numerically as a vector of p-tot elements, where the p-th element corresponds to the x-superscript-one coordinate of the p-th particle, and so on.

Post Processing Stage.

The post-processing stage is when various numerical measurements are performed on the state of the system, to extract and visualize information about simulated physical quantities. Below is a list of the kinds of measurements supported by the framework. First, visualization of the grid nodes, used for diagnostics and debugging; Figure 7.1 illustrates this type of visualization. Second, visualization of a scalar field, such as the density rho, the metric determinant, the absolute value of g, the displacement function w, and the volumetric strain epsilon; the top panels of Figure 7.4 and of Figure 7.3, panel a, illustrate this type of visualization. Third, visualization of particle scalar quantities, such as their masses, and particle vector quantities, such as their velocities and accelerations. Fourth, the profile of a scalar quantity along a path, such as the volumetric strain epsilon along the x-superscript-one dimension, which is illustrated in the bottom panels of Figure 7.4 and of Figure 7.3, panel a.

Validation.

Validation of the framework includes validating its individual components as well as end-to-end validation. The latter involves applying the framework to a well-understood configuration and comparing the outcome to the expected result; such validation is discussed in the Numerical Experiments section. Component validation consisted of verifying that all of the following procedures behave as expected: mesh generation, discretization of the derivatives, metric and Christoffel symbol computation, and density computation.

Validating the mesh was performed by generating meshes with different ranges and spacing for the edge nodes, and visualizing the result to verify that the mesh was built correctly. Figure 7.2 illustrates an example of mesh visualization. Validating the discretization of derivatives, and the computation of the metric tensor and Christoffel symbols, was performed together, because the latter two depended on the former. Once computed, the components of the metric and of the Christoffel symbols were visualized using MATLAB slice plots, to verify that these quantities have the expected form. A slice plot is used to visualize three-dimensional data through a combination of planar color-map visualizations that slice through the data at predefined locations. In addition, the above tests were performed using variable as well as fixed node spacing, to ensure that introducing node-spacing variation does not qualitatively affect the result. Density computation was validated by generating spherically distributed particles and then visualizing the resulting density through slice plots, as well as integrating the density over the volume and verifying that the result matches the total particle mass to within some tolerance.

Numerical Experiments.

Using the numerical framework described in the Numerical Framework section, below we performed two sets of numerical experiments. The first experiment is used to validate the model by simulating gravity under known conditions, namely that of our Sun, and comparing the results to the theoretical predictions of classical Newtonian gravity. The second experiment applies the framework to analyze the effects of inherent curvature on the rotation curve of the Triangulum galaxy, which is also known as Messier thirty-three, M thirty-three.

Validation of the Framework by Simulating the Sun's Gravity.

The goal of this experiment was to validate the framework by modeling the gravitational acceleration due to the Sun using the building blocks of the framework, and comparing the result to the theoretical predictions. We did not include any inherent curvature. The mesh, which is visualized in Figure 7.1, was configured with variable spacing, and initialized according to the following code.

The code snippet first defines three named constants, with a comment giving each one's meaning and units: the solar radius, about six point nine five five zero eight times ten to the eighth meters; the Sun-Earth distance, about one point four nine six times ten to the eleventh meters; and the Sun-Sedna distance, about one point three four six times ten to the thirteenth meters. It then sets the mesh extent to twenty times the Sun-Sedna distance. A comment block explains the rationale for what follows: there are three sets of edge points to get the optimal resolution with the least total number of nodes, for, first, resolving the shape of the Sun; second, resolving the volume of space containing the Sun-Earth system; and third, resolving the entire space covered by the mesh. The code then constructs the mesh: it creates a particle-mesh object sized to the mesh extent in each of the three dimensions, then adds edge points at three successive length scales, each time removing the previously added points within a smaller region and replacing them with a denser set. First, it adds centered edge points across the full mesh extent, twenty of them, using a cubic spacing function. It then removes the centered edge points within one and a half times the Sun-Earth distance, and adds twenty-five new ones there, again with cubic spacing. It then removes the centered edge points within twice the solar radius, and adds twenty new ones there, this time with a linear spacing function. Finally, the mesh is built.

The planet Sedna is considered to be at the limit of the Solar System, which is why we use twice the Sun-Sedna distance to define the extent of the mesh. The latter needs to be large, so that we can approximate the cosmic fabric strain as vanishing at the edge of the mesh, which is our assumed boundary condition.

The following code snippet introduces the Sun as a spherical density into the mesh.

The code snippet first defines four named constants: the solar mass, about one point nine eight nine times ten to the thirtieth kilograms; the solar radius, as before; the solar volume, computed as four-thirds pi times the solar radius cubed; and the solar density, computed as the solar mass divided by the solar volume. A comment then introduces the Sun's density profile as a step function: a radial coordinate r is sampled at one hundred evenly spaced points from zero to one and a half solar radii; a density array is initialized to zero at every sample point; the density is then set equal to the solar density wherever r is less than or equal to the solar radius, so that the profile is constant inside the Sun and zero outside it; and finally this radial density profile is added to the mesh as a spherical density distribution.

Figure 7.2, described in words. The figure shows the mesh used to simulate the Sun's gravity, rendered at several different length scales. The caption reads: the mesh used for simulating the Sun's gravity is shown here in perspective, panel a, and as planar views at decreasing length scales as follows: the complete region covered by the mesh, panel b; the region that includes the Sun-Earth system, panel c; and the region that includes the Sun's globe, panel d. The mesh nodes are represented as dots. Notice that at the two higher length scales, the spacing between nodes increases away from the center, which was the effect of specifying a cubic distribution of edge nodes as part of the mesh configuration.

Figure 7.3 shows the result from solving the Poisson equation for the mesh constructed above. Since the Sun's gravity is weak, see the Assumptions and Limitations section of Chapter One, its gravitational acceleration follows the classical Newtonian gravity law, according to which the acceleration a is given by two cases.

a equals minus four-thirds pi G rho-sub-sun times r, when r is less than or equal to r-sub-sun; and a equals minus G M-sub-sun, over r squared, when r is greater than r-sub-sun. This is Equation 7.27.

where r represents the distance from the Sun's center, and M-sub-sun, rho-sub-sun, and r-sub-sun are its mass, mass density, and radius, respectively. Figure 7.3, right, shows a comparison between the theoretical acceleration and that which has been simulated. The discrepancy between theory and simulation is very small, both in the vicinity of the Sun as well as at distances comparable to the Sun-Earth distance.

Figure 7.3, described in words. The figure compares the simulated and theoretical gravitational behavior near the Sun. The caption reads: panel a shows the variation of density rho and the fabric's volumetric strain in the vicinity of the Sun. Panels b and c compare the theoretical value for the gravitational acceleration due to the Sun with the simulated value that was output from simulating the Sun's gravity. The comparison is shown within two radial ranges: in the vicinity of the Sun, panel b, and in the vicinity of the Earth, panel c. The mesh used in this experiment had fifty-eight by fifty-eight by fifty-eight, equal to one hundred ninety-five thousand, one hundred twelve, nodes.

Modeling the Effect of Inherent Curvature on the Gravity of Messier Thirty-Three.

We now apply the framework to investigating the Inherent Structure Hypothesis, see Chapter Six, as a possible explanation for the Dark Matter effect. We do so by considering the rotational velocity curve, or rotation curve for short, of a given galaxy for the cases of with and without inherent curvature. For the following experiment, we chose the Triangulum galaxy, also known as Messier thirty-three, M thirty-three, because it is a relatively well-studied subject for the Dark Matter effect, per several observational studies, which effect is inferred from the rotation curve of the galaxy. For example, Figure 5 in Corbelli's study shows the rotation curve of M thirty-three decomposed into contributions due to the stellar disk, galactic gases, and the hypothetical dark matter. It is clear from this and similar other figures in the literature that, based on the contributions from ordinary matter alone, stars and gases, rotational velocities are expected to begin to decline after some distance from the center. By contrast, measurements show that rotational velocities tend to increase monotonically with their distance from the galactic center. Such a discrepancy is commonly attributed to the presence of dark matter.

About two-thirds of the ordinary, visible, matter of M thirty-three resides in the stellar disk, while the remaining third is in the form of molecular and atomic hydrogen. For the experiment herein, we used the total surface density data reported in Figure 10 of Corbelli and colleagues, which we have plotted at the bottom panels of Figure 7.4. Because most galaxies are thin disks, as is the case with M thirty-three, it is customary to report their matter density as surface density, in units of solar masses per square parsec, written M-sub-sun over p-c squared. For the purposes of the numerical simulation, however, we need volume density. Therefore, following Ciardullo and colleagues, we have assumed a constant galactic half-thickness of z-star equal to one hundred seventy-five parsecs. As Saburova and Zasov point out, such constant thickness is an idealization, but for the purposes of our comparison we assume that the effects of varying transverse thickness are insignificant.

As discussed in Chapter Six, we specify the inherent curvature via a function w equals w of x-superscript-i that maps a point x-superscript-i onto a transverse offset w along the fourth hyperspatial dimension. For our experiment we run the same mesh once with two different functions w of x-superscript-i, which are constructed according to the following code snippet.

The code snippet defines four functions, in two groups. The first group is a pair of factory functions. Make-w-fun-flat takes no arguments and returns a handle to the w-fun-flat function defined below it. Make-w-fun-MOND takes a mass parameter and returns a handle to the w-fun-MOND function, bound to that mass value.

The second group is the two underlying functions themselves. W-fun-MOND takes a position x and the mass parameter. Its documentation comment explains that it assigns a spherically symmetric transverse offset that produces a Dark Matter effect consistent with Modified Newtonian Dynamics, MOND, theory, and that the mass parameter, which is comparable to the mass of a galaxy, determines the strength of the effect, with larger values corresponding to a weaker effect. Its body computes four quantities in turn. First, the radial distance r, as the square root of the sum of the squares of the three coordinate components of x. Second, a length-scale factor s, as the square root of the gravitational constant G times the mass parameter, divided by a-naught, the MOND acceleration constant. Third and fourth, two intermediate quantities, AA and BB: AA equals the quantity r over s, squared, plus one; and BB equals four times the quantity r over s, squared, plus three. Finally, the function returns w, equal to s over two, times the square root of AA times BB, minus s over four, times the natural log of the quantity two times the square root of AA, plus the square root of BB.

W-fun-flat takes a position x. Its documentation comment explains that it assigns a transverse offset of zero to all points. Its body handles two possible input shapes: if x is a plain numeric array, it returns an array of zeros of the same size; otherwise, when x is given as a set of three coordinate components, it returns an array of zeros sized like the first of those components.

The function wfun-MOND implements the MOND-equivalent profile from Equation 6.33 and Equation 6.34, where the mass parameter is one and the same as M in Equation 6.33, and determines the length-scale factor s. For our experiment, we discovered that setting this parameter to one hundred billion solar masses, about ten times the mass of M thirty-three, resulted in the best fit of the observational data. We conducted the experiment using spherically symmetric inherent curvature. Note, however, that the overall configuration was not spherically symmetric, because the general shape of M thirty-three is a disk and not a ball.

Since this experiment involves inherent curvature, we need to be careful to distinguish between coordinate distances versus proper distances, and to be clear about when to use which. For example, all comparisons of the results obtained with and without inherent curvature are done in terms of proper distances, and so figures are labeled in terms of proper distances. At the same time, the code uses coordinate distances for internal computations. Conversion between the proper and coordinate distances was done by interpolation from a lookup table that was constructed from numerically integrating the quantity one plus w-prime.

The region of space that we consider of interest is a cube with a half-side of twenty kiloparsecs. Its size was chosen to be somewhat larger than the visible disk of M thirty-three. The total mesh extent is one hundred times larger than the region of interest, which allowed us to treat the fabric's volumetric strain at the edge of the mesh as approximately vanishing. We initialized a fifty-by-fifty-by-fifty node mesh, where nodes were distributed per the following code.

The code snippet sets the mesh extent to one hundred times the M thirty-three region-of-interest extent, then creates a particle-mesh object sized to that extent in each of the three dimensions. It first adds edge points across the mesh's full coordinate limits, twenty-five, twenty-five, and forty-three points along the first, second, and third dimensions respectively, using a cubic spacing function. It then defines a smaller coordinate range spanning from minus to plus the M thirty-three extent in the first two dimensions, and from minus to plus twice the M thirty-three half-thickness in the third dimension; removes the previously added edge points within that smaller range; and replaces them with a denser set, thirty, thirty, and ten points along the same three dimensions respectively, using a quadratic spacing function.

where spaceCube and spaceLinear specify the manner of distributing the points along each edge. A linear distribution spaces the nodes evenly, but a cubic distribution concentrates more nodes near the center and fewer near the ends of the specified range. The M thirty-three object was introduced into the mesh as a cylindrical density of uniform half-thickness z-star equal to one hundred seventy-five parsecs.

Figure 7.4 and Figure 7.5 show the results of the computation in terms of the cosmic fabric strain and the galaxy rotation curve, respectively. The rotational velocities v plotted in Figure 7.5 were calculated using the formula: v squared over r equals a-superscript-one-sub-proper, equals a-superscript-one over the square root of g-sub-one-one; where a-superscript-one equals c squared times g-bar-superscript-one-i, times partial-sub-i epsilon. This is Equation 7.28.

where a-superscript-one-sub-proper is the proper radial acceleration, and a-superscript-one is the coordinate radial acceleration, both measured in the x-superscript-one direction. The last equality is an approximation of Equation 7.20, in which the velocity terms are treated as insignificant, on the assumption that rotational velocities are much less than the speed of light, which is the case here.

Figure 7.4, described in words. The figure shows the fabric strain surrounding the M thirty-three galaxy under two scenarios. The caption reads: fabric strain due to the M thirty-three galaxy without, panel a, and with, panel b, inherent curvature. The top panels visualize a quadrant of the strain field, while the bottom panels show the density and strain profiles. The density profile used here is from Figure 10 of Corbelli and colleagues.

Figure 7.5, described in words. The figure shows the galaxy's rotation curve and the inherent-curvature profile used to produce it. The caption reads: the top panel shows M thirty-three's observed rotation curve, along with those computed with and without inherent curvature. The inherent curvature was specified by a radially symmetric hyperspatial displacement, w equals w of x-superscript-i, shown in the bottom panel, that produces a MOND-equivalent effect. The displayed fit was achieved with a scale mass of M equals ten to the eleventh power solar masses, which is about ten times the mass of M thirty-three. The bottom panel also shows the square root of g-sub-one-one, which represents the ratio between the proper distance element d-l and the coordinate element d-x-superscript-one.

These results illustrate that, for an appropriately chosen inherent curvature, the simulated velocity curve fits well with the data. The fit is quite good, although there was only one parameter to adjust for choosing the inherent curvature, the scale mass M. In principle, the inherent curvature is free to have any shape, provided it is geometrically consistent, with no kinks, intersections, or tears, so an even better fit could be achieved when a more detailed relief is chosen for the inherent spatial structure.

Discussion.

The following factors are important in the design of the numerical framework and experiments: the trade-off between reducing numerical errors versus reducing computational costs; avoiding numerical instability; and ensuring convergence. Below we discuss how each of these factors has been addressed.

Achieving an optimal trade-off between the reduction of numerical errors and computational cost relies on understanding the sources of error. For example, one such source is imprecision of the input data. It would be computationally wasteful to try to achieve numerical precision that is higher than the input precision. Another source of error is the defeaturing, or idealization, of modeled entities. Reducing this error involves using more grid nodes at the location of the entity whose feature we want to express. For example, the accurate modeling of the Sun as part of the validation experiment in the Numerical Experiments section required that more mesh nodes were introduced in its vicinity compared to the rest of the grid. The discretization of derivatives per Equations 7.7 and 7.8 introduces numerical errors by omitting higher-order terms that depend on the grid spacing. Notably, for the discretization of the second derivative, the error is linear in terms of the difference between adjacent grid-spacing values. So one must be careful not to vary the grid spacing too suddenly. Finally, numerical errors are also introduced during the post-processing stage, when the output from the solver is often interpolated to extract a meaningful outcome.

Numerical instability is a distinct source of error, which is manifested when a small perturbation in the input leads to dramatic changes in the output. We have found such instability to arise, for example, when the Q matrix that is constructed in the solution of the Poisson equation is poorly scaled. This can happen if the numerical values of the boundary coefficients, which are set to unity per Equation 7.25, differ by many orders of magnitude from the coefficients in the L matrix. To avoid this problem, we first scale all interior-node coefficients of Q, row m column n, by some fixed scale factor before setting the boundary-node coefficients to unity, and then apply this scale factor to the outcome from solving the Poisson equation. A pathological choice for the location of grid nodes can be another cause of numerical instability, such as placing them close to a location at which the node values would be singular, or nearly so.

Ensuring convergence means achieving grid fidelity beyond which adding more nodes does not qualitatively change the result of the computation, but only incrementally increases its precision. Without ensuring convergence, the meaningfulness of the numerical results is questionable. To check convergence for a given number of mesh nodes, we rerun the same calculation with a smaller number of nodes and note whether the result changes qualitatively. If the precision of the result is only incrementally decreased, then we assume convergence has been achieved.

Understanding the physics of the modeled variables also helps achieve an optimal trade-off between reducing error and computational cost. For example, the error in Equations 7.7 and 7.8 depends on the third derivative of the differentiated quantity, which happens to be the volumetric strain epsilon in the case of solving the Poisson equation. Since epsilon diminishes approximately as the inverse distance to the center of the gravitating system, the error will diminish as the inverse cube of the distance from the center of the system. This enabled us to use a cubic distribution for the edge nodes, whereby node spacing increases proportional to the square of the distance to the center.

For the category of numerical experiments where time is involved, the choice of the time step must also be considered. We use an explicit method for computing the time evolution of the system, which means that the future configuration is computed from the current one. For this type of computation, the Courant-Friedrichs-Lewy condition limits the size of the time step Delta-t, such that Delta-t is less than or equal to h over c, where h is the characteristic grid spacing and c is the speed of signal propagation, which in our case is the speed of light. An implication of the Courant-Friedrichs-Lewy condition is that whenever the grid is refined by reducing h, the time step Delta-t must also be reduced to ensure that the simulation remains convergent. Such coupling between the spatial spacing and the time step results in higher computational cost.

Summary and Conclusion.

We presented a working numerical framework for investigating the dynamics of gravitational systems in the regime of weak gravity. The framework is suited to studying the Dark Matter effect in the context of the Inherent Structure Hypothesis discussed in Chapter Six. It uses the Finite Difference Method to solve the covariant Poisson equation, which lets it account for the effects of inherent spatial curvature.

The proper functioning of the framework was validated by modeling the gravity of the Sun and comparing the result to the prediction of Newtonian gravity. The result was within sixteen percent of the theoretical prediction for a relatively coarse mesh of sixty-by-sixty-by-sixty nodes. More accurate results can be achieved in the future at the expense of more computational power, both CPU and memory.

The framework was applied to studying the effect of inherent curvature on the gravity of the stellar object Messier thirty-three, which is also known as the Triangulum Galaxy. The results, which are visualized in Figure 7.4 and Figure 7.5, demonstrated that the presence of inherent curvature does in fact cause the galaxy rotation velocities to monotonically increase with distance from the center, in a manner similar to the effect of a hypothetical dark-matter halo. Furthermore, for an appropriately chosen inherent curvature, the simulated rotation curve fitted well the observational data. This result validates the ideas presented in Chapter Six.

The primary goal of the work presented in this chapter was the development of the numerical framework, while conducting numerical experiments based on it was a secondary goal. Although the experimental results were promising, future work is needed for a thorough assessment of their significance, and for better quantification of the error by accounting for uncertainties in the input data. The framework should be extended to allow for more flexible specification of the boundary conditions, and a greater variety of galactic shapes and inherent curvatures should be investigated.

The reference list is omitted from this audio edition; see the written original.
